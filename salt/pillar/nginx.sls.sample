# Nginx Pillar 配置示例
# 建议复制为 `salt/pillar/secret/nginx.sls`，避免真实站点配置进入版本库。
# 结构保持与下方示例一致，仅在 `sites:` 下追加站点配置。

nginx:
  package: nginx
  service: nginx
  default_site: false
  client_max_body_size: 64m
  gzip: true
  sites: {}

# 示例：
#nginx:
#  sites:
#    shop1:
#      enabled: true
#      server_name:
#        - shop1.example.com
#      listen:
#        - port: 80
#        - port: 443
#          ssl: true
#      root: /var/www/shop1
#      index:
#        - index.php
#        - index.html
#      php:
#        enabled: true
#        pool: magento-shop1
#        # fastcgi_pass: unix:/run/php/php8.3-fpm-magento-shop1.sock
#      headers:
#        X-Frame-Options: SAMEORIGIN
#        X-Content-Type-Options: nosniff
#      ssl:
#        enabled: true
#        cert: /etc/letsencrypt/live/shop1.example.com/fullchain.pem
#        key: /etc/letsencrypt/live/shop1.example.com/privkey.pem
#        protocols: TLSv1.2 TLSv1.3
#        redirect: true
