import React from 'react';
import { FormattedMessage } from 'react-intl';

const FreeSummary = () => (
    <FormattedMessage
        id="payment.free.summary"
        defaultMessage="Payment: not required"
    />
);

export default FreeSummary;
